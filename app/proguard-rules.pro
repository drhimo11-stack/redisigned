# Add project specific ProGuard rules here.
# Minification is disabled by default for both build types in this project,
# so these rules are not currently applied, but are kept for future use.

-keep class com.shield.app.blocklist.** { *; }
-keep class com.shield.app.admin.** { *; }
-keep class com.shield.app.accessibility.** { *; }
