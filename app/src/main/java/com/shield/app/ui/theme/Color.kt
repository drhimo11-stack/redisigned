package com.shield.app.ui.theme

import androidx.compose.ui.graphics.Color

// حصن (Hisn — "Fortress") brand identity: an Islamic-art-inspired palette.
// Deep emerald green and gold/brass are the two colors most associated
// with Islamic art and architecture (mosque domes, calligraphy, geometric
// tilework), paired with a warm ivory text color reminiscent of aged
// parchment/manuscript pages rather than a cool tech-blue.

// --- Core brand colors ---
val ShieldEmerald = Color(0xFF12805B) // Protected / enabled / active
val ShieldEmeraldDeep = Color(0xFF0B5E43) // Same role, tuned for light backgrounds
val ShieldGold = Color(0xFFD4AF37) // Locked / needs attention — "precious, secured"
val ShieldGoldDeep = Color(0xFF9C7A1E)
val ShieldMaroon = Color(0xFFB23A2E) // Vulnerable / disabled / error
val ShieldMaroonDeep = Color(0xFF8C2C22)

// --- Dark theme neutrals (default) ---
val ShieldNightBg = Color(0xFF0A1712)
val ShieldNightSurface = Color(0xFF13241C)
val ShieldNightSurfaceElevated = Color(0xFF1C3327)
val ShieldNightOutline = Color(0xFF2E4B3C)
val ShieldIvoryText = Color(0xFFF3EDE0)
val ShieldMutedText = Color(0xFFA9BBB0)

// --- Light theme neutrals ---
val ShieldParchmentBg = Color(0xFFF6F3EA)
val ShieldParchmentSurface = Color(0xFFFFFFFF)
val ShieldParchmentSurfaceVariant = Color(0xFFE9E3D3)
val ShieldInkText = Color(0xFF1A241D)
val ShieldMutedInk = Color(0xFF5B6B60)
